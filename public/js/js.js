alert("hola");

/*function validarSlider() {
    if (Titulo.value.length == 0) { //comprueba que no esté vacío
        formregistro.codproyecto.focus();
        alert('No se agrgo un titulo');
        return false; //devolvemos el foco
    }
    if (MensajeSlider.value.length == 0) { //comprueba que no esté vacío
        formregistro.descripcion.focus();
        alert('No se agrgo un texto');
        return false;
    }
    document.getElementById("myForm").submit();
}*/